# Place this file as /etc/nginx/conf.d/00-connection-upgrade-map.conf
# so it loads inside the http { } block before site configs.
#
# Required when using: proxy_set_header Connection $connection_upgrade;

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
