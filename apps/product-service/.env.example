# Database
DATABASE_URL=postgresql://...

# Same value required on storefront + admin apps (INTERNAL_PRODUCT_API_SECRET in their .env)
INTERNAL_PRODUCT_API_SECRET=generate-a-long-random-string

# Same secret key as payment-service — used to verify balance PaymentIntents server-side
STRIPE_SECRET_KEY=sk_live_or_test_...

# Cloudflare R2 (for product images)
# Get these from: Cloudflare Dashboard → R2 → Manage R2 API Tokens
R2_ACCOUNT_ID=your_account_id
R2_ACCESS_KEY_ID=your_access_key_id
R2_SECRET_ACCESS_KEY=your_secret_access_key
R2_BUCKET_NAME=turbomeck
# Public URL for the bucket – enable "Allow public access" in R2 bucket settings to get e.g. https://pub-xxx.r2.dev
# Or use a custom domain like https://images.yoursite.com
R2_PUBLIC_URL=https://pub-xxxxxxxx.r2.dev
# Remove-background: in production, fetches are restricted to R2_PUBLIC_URL host and/or comma-separated hosts below (internal IPs always blocked).
# ALLOWED_REMOVE_BG_FETCH_HOSTS=cdn.example.com,images.example.com
# Local dev only: allow http:// URLs to your dev bucket (off by default).
# ALLOW_INSECURE_HTTP_REMOVE_BG=1
